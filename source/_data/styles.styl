// 移除站点标题背景，保留文字
.site-meta {
  background: none !important;
}

.brand {
  background: none !important;
}

.site-title {
  background: none !important;
  color: #333 !important; // 确保文字颜色可见，可以根据需要调整颜色
  padding: 0 !important;
  display: inline-block !important; // 确保文字显示
  // 如果需要，可以在这里添加其他样式
  // font-size: 24px; // 调整字体大小
}